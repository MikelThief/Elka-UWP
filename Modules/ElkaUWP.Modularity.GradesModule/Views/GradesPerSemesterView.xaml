<Page
    x:Class="ElkaUWP.Modularity.GradesModule.Views.GradesPerSemesterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ElkaUWP.Modularity.GradesModule.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tcontrols="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:entities="using:ElkaUWP.DataLayer.Propertiary.Entities"
    xmlns:converters="using:ElkaUWP.Modularity.GradesModule.Models.Converters"
    xmlns:convertersInf="using:ElkaUWP.Infrastructure.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <ResourceDictionary>
            <converters:FinishedSubjectPassedStatusToBrushConverter x:Key="FinishedSubjectPassedStatusToBrushConverter"/>
            <convertersInf:HashSetToStringConverter x:Key="HashSetToStringConverter"/>
            <DataTemplate x:DataType="entities:SubjectApproach" x:Key="FinishedSubjectDataTemplate">
                <Grid Padding="8" BorderBrush="{x:Bind IsPassed, Converter={StaticResource FinishedSubjectPassedStatusToBrushConverter}}" BorderThickness="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="78" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Vertical" Grid.Column="0" Spacing="5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
                            <TextBlock Text="{x:Bind Acronym}" VerticalAlignment="Center" Style="{StaticResource TitleTextBlockStyle}"/>
                            <TextBlock Text="-" VerticalAlignment="Center" Style="{StaticResource TitleTextBlockStyle}"/>
                            <TextBlock Text="{x:Bind SemesterLiteral}" VerticalAlignment="Bottom" Style="{StaticResource TitleTextBlockStyle}"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Bottom" Style="{StaticResource BaseTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="WrapWholeWords"/>
                        <TextBlock MinHeight="35" HorizontalAlignment="Center" Text="{x:Bind StaffHashSet, Converter={StaticResource HashSetToStringConverter}}" VerticalAlignment="Bottom" Style="{StaticResource CaptionTextBlockStyle}" Foreground="DarkGray"  />

                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{x:Bind GradeLiteral}" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource HeaderTextBlockStyle}"/>

                </Grid>
            </DataTemplate>
            <DataTemplate x:DataType="entities:SubjectApproach" x:Key="InProgressSubjectDataTemplate">
                <Grid CornerRadius="3" Background="LightBlue">
                    <TextBlock Text="{x:Bind Acronym}" VerticalAlignment="Top" HorizontalAlignment="Left" TextWrapping="NoWrap" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock Text="{x:Bind SemesterLiteral}" VerticalAlignment="Bottom" HorizontalAlignment="Right" TextWrapping="NoWrap" Style="{StaticResource CaptionTextBlockStyle}" />
                </Grid>
            </DataTemplate>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///ElkaUWP.Infrastructure/ResourceDictionaries/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid x:Name="PageGrid">
        <!-- Head -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="MediumState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource MediumWindowSnapPoint}"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="GradesPerSemesterCommandBar.DefaultLabelPosition" Value="Right" />

                        <Setter Target="GradesPerSemesterPageHeader.Padding" Value="24 4 0 0"/>

                        <Setter Target="ContentArea.Margin" Value="24"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="SmallState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource SmallWindowSnapPoint}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                        <Setter Target="GradesPerSemesterCommandBar.DefaultLabelPosition" Value="Bottom" />

                        <Setter Target="GradesPerSemesterPageHeader.Padding" Value="96 4 0 0"/>

                        <Setter Target="ContentArea.Margin" Value="12"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Body -->
        <CommandBar x:Name="GradesPerSemesterCommandBar" Grid.Row="0" IsDynamicOverflowEnabled="True" DefaultLabelPosition="Bottom" Background="{ThemeResource SystemControlAcrylicElementBrush}">

            <AppBarButton Icon="Refresh" Name="GradesPerSemesterRefreshButton" x:Uid="GradesPerSemesterRefreshButton"/>

            <CommandBar.Content>
                <TextBlock Name="GradesPerSemesterPageHeader" Style="{StaticResource SubtitleTextBlockStyle}" x:Uid="GradesPerSemesterPageHeader" />
            </CommandBar.Content>

        </CommandBar>

        <ScrollViewer Grid.Row="1" x:Name="ContentArea" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
            <StackPanel>
                <TextBlock x:Uid="SubjectsGradesViewInProgressHeader" Style="{StaticResource SubheaderTextBlockStyle}"/>
                    <tcontrols:AdaptiveGridView ItemsSource="{x:Bind ViewModel.InProgressSubjectApproaches}" ItemTemplate="{StaticResource InProgressSubjectDataTemplate}">
                        <tcontrols:AdaptiveGridView.ItemContainerStyle>
                            <Style TargetType="FrameworkElement">
                                <Setter Property="Margin" Value="8"/>
                                <Setter Property="MinWidth" Value="300"/>
                            </Style>
                        </tcontrols:AdaptiveGridView.ItemContainerStyle>
                    </tcontrols:AdaptiveGridView>
                <TextBlock x:Uid="SubjectsGradesViewFinishedHeader" Style="{StaticResource SubheaderTextBlockStyle}"/>
                <tcontrols:AdaptiveGridView ItemsSource="{x:Bind ViewModel.FinishedSubjectApproaches}" ItemTemplate="{StaticResource FinishedSubjectDataTemplate}">
                    <tcontrols:AdaptiveGridView.ItemContainerStyle>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="8"/>
                            <Setter Property="MinWidth" Value="350"/>
                            <Setter Property="MinHeight" Value="100"/>
                        </Style>
                    </tcontrols:AdaptiveGridView.ItemContainerStyle>
                </tcontrols:AdaptiveGridView>
            </StackPanel>



        </ScrollViewer>


    </Grid>
</Page>
